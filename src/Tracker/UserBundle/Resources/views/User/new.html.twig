{% extends '::base.html.twig' %}

{% form_theme form 'TrackerProjectBundle:Form:fields.html.twig' %}

{% block body -%}
    <h3>{{ "user.page.create.title"|trans }}</h3>

    <p>
        <a href="{{ path('user') }}">
            {{ "user.action.backToList"|trans }}
        </a>
    </p>

    {{ form(form, {'attr':{'class':"form-horizontal"}}) }}

{% endblock %}
