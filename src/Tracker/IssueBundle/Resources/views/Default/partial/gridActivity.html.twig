{% import "TrackerHomeBundle::macros.html.twig" as UI %}

<table class="table table-bordered">
    <thead>
    <tr>
        <th>  {{ "activity.grid.event"|trans }}</th>
        <th>  {{ "activity.grid.user"|trans }}</th>
        <th>  {{ "activity.grid.createAt"|trans }}</th>
    </tr>
    </thead>
    <tbody>
    {% for activity in activities %}
            <tr>
                <td>{{ activity.event|trans }}</a></td>
	            <td>
		            {{ UI.renderLinkUserPage(activity.user) }}
	            </td>
	            <td>{{ UI.formatDateAndTimeZone(activity.created) }}</td>
            </tr>
    {% endfor %}
    </tbody>
</table>
